# Defacto v0.43 - Major Language Update 🚀

## Дата выпуска: 28 Февраля 2026

## 🎉 Что нового

### ✅ Сложные выражения

Теперь поддерживаются многосоставные выражения:

```de
var x: i32 = 10
var y: i32 = 5
var result: i32 = 0

result = (x + y)           // 15
result = ((x + y) * 2)     // 30
result = ((a * b) + (c / d))  // Композитные выражения
```

**Приоритет операторов:**
1. `*`, `/` (умножение/деление)
2. `+`, `-` (сложение/вычитание)
3. `==`, `!=`, `<`, `>`, `<=`, `>=` (сравнение)
4. `&&` (логическое И)
5. `||` (логическое ИЛИ)

### ✅ Отрицательные литералы

```de
var neg: i32 = -42
var temp: i32 = -1
```

### ✅ Bool тип

```de
var flag: bool = true
var other: bool = false

if flag {
    display{flag is true}
}
```

### ✅ Логические операторы

```de
var a: bool = true
var b: bool = false

if a && b {
    display{both true}
}

if a || b {
    display{at least one true}
}

if !a {
    display{a is false}
}
```

### ✅ Continue в циклах

```de
loop {
    if x == 5 {
        continue  // Пропустить итерацию
    }
    display{x}
}
```

### ✅ Перечисления (enum)

```de
enum Color {
    Red,      // 0
    Green,    // 1
    Blue      // 2
}

enum Status {
    Ok = 1,
    Error = 2
}
```

### ✅ Инициализация массивов

```de
var arr: i32[5] = [1, 2, 3, 4, 5]
var flags: bool[3] = [true, false, true]
```

### ✅ Функции с параметрами (parser)

```de
function == add {
    <.de
        var a: i32
        var b: i32
        static.pl>
        // Параметры доступны как переменные
    .>
}
```

## 📦 Установка

### macOS (Homebrew)

```bash
brew tap vivooifo-droid/Defacto
brew install defacto
```

### Linux

```bash
sudo apt update
sudo apt install build-essential nasm
cd compiler && make
```

## 🧪 Тестирование

```bash
# Компиляция теста v0.43
./defacto test-v043.de -o test
./test

# Ожидаемый вывод:
# 30
# 10
# -42 (как unsigned)
```

## 📝 Изменения в файлах

### Обновлённые файлы:
- `compiler/src/defacto.h` — новые токены и типы узлов
- `compiler/src/lexer.h` — `bool`, `true`, `false`, `&&`, `||`, `!`
- `compiler/src/parser.h` — парсинг выражений, bool, enum, continue
- `compiler/src/codegen.h` — codegen для continue, логических операторов
- `README.md` — обновлено до v0.43
- `README.ru.md` — обновлено до v0.43

## ⚠️ Известные ограничения

### Частичная поддержка:
- Логические операторы `&&`, `||` требуют short-circuit evaluation (в разработке)
- Инициализация массивов с запятыми работает только после `static.pl>`
- Функции с параметрами требуют доработки codegen

### Не поддерживается:
- Дженерики
- Замыкания
- Pattern matching
- Модули

## 🎯 Планы на v0.44

- [ ] Short-circuit evaluation для `&&` и `||`
- [ ] Функции с параметрами (полная поддержка)
- [ ] Возвращаемые типы функций
- [ ] Улучшенная работа с массивами
- [ ] Стандартная библиотека

## 📚 Примеры

### Complex expressions
```de
var result: i32 = ((10 + 5) * 2)  // 30
```

### Bool and logic
```de
var is_valid: bool = true
var has_permission: bool = false

if is_valid && has_permission {
    display{access granted}
} else if is_valid || has_permission {
    display{partial access}
}
```

### Continue in loops
```de
var i: i32 = 0
while i < 10 {
    if i == 5 {
        i = (i + 1)
        continue
    }
    display{i}
    i = (i + 1)
}
```

### Enum
```de
enum Direction {
    North,
    South,
    East,
    West
}
```

---

**Спасибо за использование Defacto!** 🎉
